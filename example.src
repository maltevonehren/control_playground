
plant = tf([0, 0.5, 0.5], [1, -1.5, 0.7]);
plant;
step(plant);

Kp = 0.25;
Ki = 0.05;
controller = tf([Kp, -Kp + Ki], [1, -1]);
controller;

sys = {
  controller(u);
  p = plant(controller);
};
sys;
step(sys);


load("out.csv");
